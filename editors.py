class Editor:
    def BigStartswithEmail(accounts):
        for each in accounts:
            if each[0].isupper(): yield each
    def SmallStartswithEmail(accounts):
        for each in accounts:
            if each[0].islower(): yield each



